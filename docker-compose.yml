version: "3.9"

services:
  presensi:
    image: ghcr.io/noc-upaznet/presensi:latest
    ports:
      - "8083:80"
    environment:
      APP_ENV: production
      APP_DEBUG: "false"
    networks:
      - presensi

networks:
  presensi:
