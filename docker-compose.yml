version: "3.9"

services:

  php:
    image: ghcr.io/noc-upaznet/presensi:latest
    environment:
      APP_ENV: production
      APP_DEBUG: "false"
    networks:
      - presensi

  services:
  nginx:
    image: presensi-nginx:latest
    ports:
      - "8083:80"
    depends_on:
      - php
    deploy:
      placement:
        constraints:
          - node.hostname == docker-node1
    networks:
      - presensi

  redis:
    image: redis:alpine
    networks:
      - presensi

networks:
  presensi:

volumes:
  mysql-data:

configs:
  nginx_conf:
    file: docker/nginx/default.conf
